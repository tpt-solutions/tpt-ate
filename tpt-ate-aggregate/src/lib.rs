//! Outcome aggregation for the test & assembly stage: extends RFC-002's `OutcomeReport`
//! file exchange with bin/yield data (`TestOutcome`) instead of defining a new channel.
//! See `spec.txt` Section 2C and `tpt-silicon/spec3.txt` Section 4.
